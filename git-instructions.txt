git status
git add .
git commit -m "update gallery"
git push origin main



If There Are Conflicts
git pull origin main




### If You Want to Force Your Local Version

If you're certain your local version is the one you want to keep (overwriting any changes on GitHub), you can force push:
git push -f origin main
